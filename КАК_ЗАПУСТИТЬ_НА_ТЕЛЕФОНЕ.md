# 📱 Как запустить приложение на телефоне или отправить друзьям

## Способ 1: Через локальную сеть (самый простой) ⭐

### Шаг 1: Узнайте IP адрес вашего компьютера

**Windows:**
```bash
ipconfig
```
Найдите "IPv4-адрес" (обычно что-то вроде `192.168.1.100`)

**Или через PowerShell:**
```powershell
(Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -notlike "*Loopback*"}).IPAddress
```

### Шаг 2: Запустите сервер с доступом из сети

В папке приложения выполните:
```bash
python -m http.server 8000 --bind 0.0.0.0
```

### Шаг 3: Откройте на телефоне

1. Убедитесь, что телефон подключен к той же Wi-Fi сети, что и компьютер
2. Откройте браузер на телефоне
3. Введите адрес: `http://ВАШ_IP:8000`
   Например: `http://192.168.1.100:8000`

### Шаг 4: Установите на телефон

После открытия в браузере:
- **Android (Chrome)**: Меню (⋮) → "Добавить на главный экран"
- **iOS (Safari)**: Поделиться → "На экран «Домой»"

---

## Способ 2: Через облачный хостинг (для отправки друзьям) 🌐

### Вариант A: GitHub Pages (бесплатно)

1. Создайте аккаунт на [GitHub.com](https://github.com)
2. Создайте новый репозиторий (например, `spe-tickets`)
3. Загрузите все файлы приложения в репозиторий
4. Перейдите в Settings → Pages
5. Выберите ветку `main` и папку `/root`
6. Через несколько минут приложение будет доступно по адресу:
   `https://ВАШ_НИК.github.io/spe-tickets`

### Вариант B: Netlify Drop (самый быстрый)

1. Откройте [app.netlify.com/drop](https://app.netlify.com/drop)
2. Перетащите всю папку приложения в браузер
3. Получите ссылку типа `https://random-name-123.netlify.app`
4. Отправьте ссылку друзьям!

### Вариант C: Vercel

1. Установите Vercel CLI: `npm i -g vercel`
2. В папке приложения выполните: `vercel`
3. Следуйте инструкциям
4. Получите ссылку для доступа

---

## Способ 3: Через USB/файлы (офлайн) 📁

### Для Android:

1. Скопируйте всю папку приложения на телефон (через USB или облако)
2. Установите приложение "Simple HTTP Server" или "KSWEB" из Google Play
3. В приложении выберите папку с приложением
4. Запустите сервер
5. Откройте указанный адрес в браузере

### Для iOS:

1. Используйте приложение "Server Auditor" или "Working Copy"
2. Или используйте способ 1 (локальная сеть)

---

## Способ 4: Создать APK (только для Android) 📦

Можно использовать инструменты вроде:
- **PWA Builder** (pwabuilder.com) - конвертирует PWA в APK
- **Bubble** или другие PWA-to-APK конвертеры

---

## 🔥 Рекомендация

**Для быстрого тестирования:** Используйте Способ 1 (локальная сеть)

**Для отправки друзьям:** Используйте Способ 2B (Netlify Drop) - самый быстрый и простой!

---

## ⚠️ Важные моменты

1. **Файл tickets.json должен быть в папке** - без него приложение не заработает
2. **Иконки (icon-192.png, icon-512.png)** - нужны для установки на главный экран
3. **HTTPS требуется для PWA** - на localhost это работает, но для публичного доступа нужен HTTPS (Netlify, Vercel предоставляют его бесплатно)

---

## 🚀 Быстрая команда для локального доступа

Создайте файл `start_server.bat` (Windows) с содержимым:

```batch
@echo off
echo Запуск сервера для доступа с телефона...
echo.
echo Узнайте IP адрес вашего компьютера:
ipconfig | findstr /i "IPv4"
echo.
echo Затем на телефоне откройте: http://ВАШ_IP:8000
echo.
python -m http.server 8000 --bind 0.0.0.0
pause
```

Запустите этот файл двойным кликом!

