# СПЭ Билеты - Мобильное приложение

Мобильное приложение для изучения билетов с карточками. Можно установить на телефон как PWA (Progressive Web App).

## 🚀 Быстрый старт

### 1. Подготовка данных

1. Установите зависимости Python:
```bash
python -m pip install -r requirements.txt
```

2. Конвертируйте ваш Excel файл в JSON:
```bash
python convert_excel_to_json.py путь_к_вашему_файлу.xlsx
```

Или просто запустите скрипт и следуйте инструкциям:
```bash
python convert_excel_to_json.py
```

**Важно:** Excel файл должен содержать столбцы:
- `Номер билета` (или `номер билета`)
- `Вопрос` (или `вопрос`)
- `Ответ` (или `ответ`)

Данные должны начинаться со второй строки (первая строка - названия столбцов).

### 2. Запуск приложения

#### Вариант 1: Локальный сервер (для разработки)

Откройте терминал в папке приложения и запустите:

**Python 3:**
```bash
python -m http.server 8000
```

**Python 2:**
```bash
python -m SimpleHTTPServer 8000
```

Затем откройте в браузере: `http://localhost:8000`

#### Вариант 2: Прямое открытие

Просто откройте файл `index.html` в браузере (может не работать из-за CORS при загрузке JSON).

### 3. Установка на телефон

#### Android (Chrome):
1. Откройте приложение в браузере Chrome
2. Нажмите меню (три точки)
3. Выберите "Добавить на главный экран" или "Установить приложение"
4. Приложение появится на главном экране

#### iOS (Safari):
1. Откройте приложение в Safari
2. Нажмите кнопку "Поделиться"
3. Выберите "На экран «Домой»"
4. Приложение появится на главном экране

## 📱 Функции

- ✅ **Карточки с вопросами и ответами**
- 🔄 **Переворот карточки** (кнопка или свайп вправо)
- ⏭️ **Пропуск билета** (кнопка или свайп влево)
- 📊 **Прогресс изучения**
- 💾 **Автосохранение прогресса** (localStorage)
- 📱 **Полностью адаптивный дизайн**
- 🎨 **Красивый современный интерфейс**
- ⌨️ **Поддержка клавиатуры** (стрелки, пробел, Enter)

## 🎮 Управление

- **Перевернуть карточку**: Кнопка "Перевернуть" или свайп вправо, или клавиша Enter/←
- **Следующий билет**: Кнопка "Пропустить" или свайп влево, или клавиша →/Пробел

## 📁 Структура файлов

```
СПЭ_Билеты_приложение/
├── index.html          # Главная страница
├── styles.css          # Стили
├── app.js              # Логика приложения
├── manifest.json       # PWA манифест
├── sw.js               # Service Worker
├── tickets.json        # Данные билетов (создается из Excel)
├── convert_excel_to_json.py  # Скрипт конвертации
├── create_icons.py     # Скрипт создания иконок (опционально)
├── generate_icons.html  # Генератор иконок в браузере
├── icon-192.png        # Иконка 192x192 (создается)
├── icon-512.png        # Иконка 512x512 (создается)
├── requirements.txt    # Python зависимости
└── README.md           # Этот файл
```

## 🔧 Настройка

### Изменение темы

Отредактируйте цвета в `styles.css`:
- Градиент фона: `background: linear-gradient(...)`
- Цвет кнопок: `.btn-skip`, `.btn-flip`
- Цвет темы: `theme-color` в `manifest.json` и `index.html`

### Изменение иконок

**Вариант 1: Генератор иконок (рекомендуется)**
1. Откройте файл `generate_icons.html` в браузере
2. Нажмите кнопки для скачивания иконок
3. Сохраните файлы `icon-192.png` и `icon-512.png` в папку приложения

**Вариант 2: Python скрипт**
```bash
python -m pip install Pillow
python create_icons.py
```

**Вариант 3: Вручную**
Замените файлы `icon-192.png` и `icon-512.png` на свои иконки (192x192 и 512x512 пикселей).

## ❓ Решение проблем

### Файл tickets.json не найден
- Убедитесь, что вы запустили скрипт конвертации
- Проверьте, что файл `tickets.json` находится в той же папке, что и `index.html`

### Приложение не работает офлайн
- Убедитесь, что Service Worker зарегистрирован (проверьте консоль браузера)
- Очистите кэш и перезагрузите страницу

### Данные не отображаются
- Проверьте формат JSON файла
- Убедитесь, что названия столбцов совпадают: "Номер билета", "Вопрос", "Ответ"
- Откройте консоль браузера (F12) для просмотра ошибок

## 📝 Лицензия

Свободное использование для личных целей.

